# Core
aiohttp>=3.9
asyncio-throttle>=1.0
python-dotenv>=1.0

# Email parsing
mail-parser>=3.15
# lxml is pulled transitively via mail-parser. Pinned floor to 6.1.0 to
# close CVE-2026-41066 (XXE / local-file read via default resolve_entities).
lxml>=6.1.0
dkimpy>=1.1
checkdmarc>=5.0
dnspython>=2.4

# QR / Image
pyzbar>=0.1.9
opencv-python-headless>=4.8
Pillow>=10.0
PyMuPDF>=1.23
python-docx>=1.0

# URL / Domain
python-whois>=0.9
tldextract>=5.0

# Browser automation (URL detonation)
playwright>=1.40

# LLM
anthropic>=0.34

# NLP / ML
scikit-learn>=1.3
joblib>=1.3
numpy>=1.25

# API framework
fastapi>=0.104
uvicorn>=0.24
python-multipart>=0.0.9
pydantic>=2.5

# Storage
sqlalchemy>=2.0
aiosqlite>=0.19

# IOC export
stix2>=3.0

# Reporting
jinja2>=3.1

# Config loading. PipelineConfig.from_env() delegates to from_yaml() when
# config.yaml is present in CWD, which is the default for both local runs
# and the Docker image (the file is COPY'd in). This has to be a declared
# runtime dep, not optional, to match actual runtime behaviour.
pyyaml>=6.0

# Security: HTML sanitization for body_html rendering (defense in depth
# alongside the sandboxed iframe). See src/security/html_sanitizer.py.
bleach>=6.0

# Testing
pytest>=7.4
pytest-asyncio>=0.23
pytest-mock>=3.12
aioresponses>=0.7
httpx>=0.25

# Code quality
flake8>=6.1
mypy>=1.7

# QR code generation (for tests)
qrcode>=7.4
